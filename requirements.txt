# need this to run the code successfully. 
sentence-transformers>=2.2.0
rapidfuzz>=2.13.0
pandas>=1.3.0
scikit-learn>=1.3.0
numpy>=1.20.0
torch>=1.8.0
transformers>=4.22.0  # Required for BiomedBERT with its new naming scheme
# BiomedBERT model (formerly known as PubMedBERT) is loaded automatically by sentence-transformers
